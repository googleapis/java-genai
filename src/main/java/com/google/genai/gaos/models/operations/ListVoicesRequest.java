/*
 * Copyright 2026 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.google.genai.gaos.models.operations;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.google.genai.gaos.utils.SpeakeasyMetadata;
import com.google.genai.gaos.utils.Utils;
import jakarta.annotation.Nullable;
import java.lang.Integer;
import java.lang.Override;
import java.lang.String;
import java.util.List;
import java.util.Optional;


public class ListVoicesRequest {
    /**
     * API version for request routing.
     */
    @SpeakeasyMetadata("pathParam:style=simple,explode=false,name=api_version")
    private String apiVersion;

    /**
     * Optional. Filter by accent description (e.g. "American", "British").
     * Case-insensitive exact match. If multiple values are specified, matches
     * voices with any of the specified accents (OR).
     */
    @SpeakeasyMetadata("queryParam:style=form,explode=true,name=accent")
    private List<String> accent;

    /**
     * Optional. Filter by intended context or domain (e.g. "News, Commercial").
     * Case-insensitive exact match. If multiple values are specified, matches
     * voices with any of the specified contexts (OR).
     */
    @SpeakeasyMetadata("queryParam:style=form,explode=true,name=context")
    private List<String> contexts;

    /**
     * Optional. Filter by gender presentation (e.g. "female", "male", "neutral").
     * Case-insensitive exact match. If multiple values are specified, matches
     * voices with any of the specified genders (OR).
     */
    @SpeakeasyMetadata("queryParam:style=form,explode=true,name=gender")
    private List<String> gender;

    /**
     * Optional. Filter by BCP-47 language code (e.g. "en-US").
     * Case-insensitive exact match. If multiple values are specified, matches
     * voices with any of the specified language codes (OR).
     */
    @SpeakeasyMetadata("queryParam:style=form,explode=true,name=language_code")
    private List<String> languageCode;

    /**
     * Optional. The maximum number of voices to return per page. The service may
     * return fewer than this value. If unspecified, at most 50 voices are
     * returned. The maximum value is 1000; values above 1000 are coerced to
     * 1000.
     */
    @SpeakeasyMetadata("queryParam:style=form,explode=true,name=page_size")
    private Integer pageSize;

    /**
     * Optional. A page token received from a previous `ListVoices` call. Provide
     * this to retrieve the subsequent page.
     * 
     * <p>When paginating, all filter query parameters (`language_code`,
     * `region_code`, `accent`, `persona`, `context`, `gender`, `pitch`, `type`,
     * and `search`) must match the call that returned this token; otherwise the
     * request fails with `INVALID_ARGUMENT`. `page_size` may change between
     * pages.
     */
    @SpeakeasyMetadata("queryParam:style=form,explode=true,name=page_token")
    private String pageToken;

    /**
     * Optional. Filter by vocal persona (e.g. "Warm, Friendly").
     * Case-insensitive exact match. If multiple values are specified, matches
     * voices with any of the specified personas (OR).
     */
    @SpeakeasyMetadata("queryParam:style=form,explode=true,name=persona")
    private List<String> persona;

    /**
     * Optional. Filter by vocal pitch. Accepts `"low"`, `"medium"`, `"high"`
     * (case-insensitive). If multiple values are specified, matches voices with
     * any of the specified pitches (OR).
     */
    @SpeakeasyMetadata("queryParam:style=form,explode=true,name=pitch")
    private List<String> pitch;

    /**
     * Optional. Filter by ISO 3166-1 alpha-2 or UN M.49 region code (e.g. "US",
     * "001").
     * Case-insensitive exact match. If multiple values are specified, matches
     * voices with any of the specified region codes (OR).
     */
    @SpeakeasyMetadata("queryParam:style=form,explode=true,name=region_code")
    private List<String> regionCode;

    /**
     * Optional. Free-text substring search query matched case-insensitively
     * against both `display_name` and `description`. Maximum 2048 bytes.
     */
    @SpeakeasyMetadata("queryParam:style=form,explode=true,name=search")
    private String search;

    /**
     * Optional. Filter by voice type. Accepts `"prebuilt"`, `"replicated"`,
     * `"prompted"` (case-insensitive). If multiple values are specified, matches
     * voices with any of the specified types (OR).
     */
    @SpeakeasyMetadata("queryParam:style=form,explode=true,name=type")
    private List<String> type;

    @JsonCreator
    public ListVoicesRequest(
            @Nullable String apiVersion,
            @Nullable List<String> accent,
            @Nullable List<String> contexts,
            @Nullable List<String> gender,
            @Nullable List<String> languageCode,
            @Nullable Integer pageSize,
            @Nullable String pageToken,
            @Nullable List<String> persona,
            @Nullable List<String> pitch,
            @Nullable List<String> regionCode,
            @Nullable String search,
            @Nullable List<String> type) {
        this.apiVersion = apiVersion;
        this.accent = accent;
        this.contexts = contexts;
        this.gender = gender;
        this.languageCode = languageCode;
        this.pageSize = pageSize;
        this.pageToken = pageToken;
        this.persona = persona;
        this.pitch = pitch;
        this.regionCode = regionCode;
        this.search = search;
        this.type = type;
    }
    
    public ListVoicesRequest() {
        this(null, null, null,
            null, null, null,
            null, null, null,
            null, null, null);
    }

    /**
     * API version for request routing.
     */
    public Optional<String> apiVersion() {
        return Optional.ofNullable(this.apiVersion);
    }

    /**
     * Optional. Filter by accent description (e.g. "American", "British").
     * Case-insensitive exact match. If multiple values are specified, matches
     * voices with any of the specified accents (OR).
     */
    public Optional<List<String>> accent() {
        return Optional.ofNullable(this.accent);
    }

    /**
     * Optional. Filter by intended context or domain (e.g. "News, Commercial").
     * Case-insensitive exact match. If multiple values are specified, matches
     * voices with any of the specified contexts (OR).
     */
    public Optional<List<String>> contexts() {
        return Optional.ofNullable(this.contexts);
    }

    /**
     * Optional. Filter by gender presentation (e.g. "female", "male", "neutral").
     * Case-insensitive exact match. If multiple values are specified, matches
     * voices with any of the specified genders (OR).
     */
    public Optional<List<String>> gender() {
        return Optional.ofNullable(this.gender);
    }

    /**
     * Optional. Filter by BCP-47 language code (e.g. "en-US").
     * Case-insensitive exact match. If multiple values are specified, matches
     * voices with any of the specified language codes (OR).
     */
    public Optional<List<String>> languageCode() {
        return Optional.ofNullable(this.languageCode);
    }

    /**
     * Optional. The maximum number of voices to return per page. The service may
     * return fewer than this value. If unspecified, at most 50 voices are
     * returned. The maximum value is 1000; values above 1000 are coerced to
     * 1000.
     */
    public Optional<Integer> pageSize() {
        return Optional.ofNullable(this.pageSize);
    }

    /**
     * Optional. A page token received from a previous `ListVoices` call. Provide
     * this to retrieve the subsequent page.
     * 
     * <p>When paginating, all filter query parameters (`language_code`,
     * `region_code`, `accent`, `persona`, `context`, `gender`, `pitch`, `type`,
     * and `search`) must match the call that returned this token; otherwise the
     * request fails with `INVALID_ARGUMENT`. `page_size` may change between
     * pages.
     */
    public Optional<String> pageToken() {
        return Optional.ofNullable(this.pageToken);
    }

    /**
     * Optional. Filter by vocal persona (e.g. "Warm, Friendly").
     * Case-insensitive exact match. If multiple values are specified, matches
     * voices with any of the specified personas (OR).
     */
    public Optional<List<String>> persona() {
        return Optional.ofNullable(this.persona);
    }

    /**
     * Optional. Filter by vocal pitch. Accepts `"low"`, `"medium"`, `"high"`
     * (case-insensitive). If multiple values are specified, matches voices with
     * any of the specified pitches (OR).
     */
    public Optional<List<String>> pitch() {
        return Optional.ofNullable(this.pitch);
    }

    /**
     * Optional. Filter by ISO 3166-1 alpha-2 or UN M.49 region code (e.g. "US",
     * "001").
     * Case-insensitive exact match. If multiple values are specified, matches
     * voices with any of the specified region codes (OR).
     */
    public Optional<List<String>> regionCode() {
        return Optional.ofNullable(this.regionCode);
    }

    /**
     * Optional. Free-text substring search query matched case-insensitively
     * against both `display_name` and `description`. Maximum 2048 bytes.
     */
    public Optional<String> search() {
        return Optional.ofNullable(this.search);
    }

    /**
     * Optional. Filter by voice type. Accepts `"prebuilt"`, `"replicated"`,
     * `"prompted"` (case-insensitive). If multiple values are specified, matches
     * voices with any of the specified types (OR).
     */
    public Optional<List<String>> type() {
        return Optional.ofNullable(this.type);
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * API version for request routing.
     */
    public ListVoicesRequest withApiVersion(@Nullable String apiVersion) {
        this.apiVersion = apiVersion;
        return this;
    }


    /**
     * Optional. Filter by accent description (e.g. "American", "British").
     * Case-insensitive exact match. If multiple values are specified, matches
     * voices with any of the specified accents (OR).
     */
    public ListVoicesRequest withAccent(@Nullable List<String> accent) {
        this.accent = accent;
        return this;
    }


    /**
     * Optional. Filter by intended context or domain (e.g. "News, Commercial").
     * Case-insensitive exact match. If multiple values are specified, matches
     * voices with any of the specified contexts (OR).
     */
    public ListVoicesRequest withContexts(@Nullable List<String> contexts) {
        this.contexts = contexts;
        return this;
    }


    /**
     * Optional. Filter by gender presentation (e.g. "female", "male", "neutral").
     * Case-insensitive exact match. If multiple values are specified, matches
     * voices with any of the specified genders (OR).
     */
    public ListVoicesRequest withGender(@Nullable List<String> gender) {
        this.gender = gender;
        return this;
    }


    /**
     * Optional. Filter by BCP-47 language code (e.g. "en-US").
     * Case-insensitive exact match. If multiple values are specified, matches
     * voices with any of the specified language codes (OR).
     */
    public ListVoicesRequest withLanguageCode(@Nullable List<String> languageCode) {
        this.languageCode = languageCode;
        return this;
    }


    /**
     * Optional. The maximum number of voices to return per page. The service may
     * return fewer than this value. If unspecified, at most 50 voices are
     * returned. The maximum value is 1000; values above 1000 are coerced to
     * 1000.
     */
    public ListVoicesRequest withPageSize(@Nullable Integer pageSize) {
        this.pageSize = pageSize;
        return this;
    }


    /**
     * Optional. A page token received from a previous `ListVoices` call. Provide
     * this to retrieve the subsequent page.
     * 
     * <p>When paginating, all filter query parameters (`language_code`,
     * `region_code`, `accent`, `persona`, `context`, `gender`, `pitch`, `type`,
     * and `search`) must match the call that returned this token; otherwise the
     * request fails with `INVALID_ARGUMENT`. `page_size` may change between
     * pages.
     */
    public ListVoicesRequest withPageToken(@Nullable String pageToken) {
        this.pageToken = pageToken;
        return this;
    }


    /**
     * Optional. Filter by vocal persona (e.g. "Warm, Friendly").
     * Case-insensitive exact match. If multiple values are specified, matches
     * voices with any of the specified personas (OR).
     */
    public ListVoicesRequest withPersona(@Nullable List<String> persona) {
        this.persona = persona;
        return this;
    }


    /**
     * Optional. Filter by vocal pitch. Accepts `"low"`, `"medium"`, `"high"`
     * (case-insensitive). If multiple values are specified, matches voices with
     * any of the specified pitches (OR).
     */
    public ListVoicesRequest withPitch(@Nullable List<String> pitch) {
        this.pitch = pitch;
        return this;
    }


    /**
     * Optional. Filter by ISO 3166-1 alpha-2 or UN M.49 region code (e.g. "US",
     * "001").
     * Case-insensitive exact match. If multiple values are specified, matches
     * voices with any of the specified region codes (OR).
     */
    public ListVoicesRequest withRegionCode(@Nullable List<String> regionCode) {
        this.regionCode = regionCode;
        return this;
    }


    /**
     * Optional. Free-text substring search query matched case-insensitively
     * against both `display_name` and `description`. Maximum 2048 bytes.
     */
    public ListVoicesRequest withSearch(@Nullable String search) {
        this.search = search;
        return this;
    }


    /**
     * Optional. Filter by voice type. Accepts `"prebuilt"`, `"replicated"`,
     * `"prompted"` (case-insensitive). If multiple values are specified, matches
     * voices with any of the specified types (OR).
     */
    public ListVoicesRequest withType(@Nullable List<String> type) {
        this.type = type;
        return this;
    }


    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ListVoicesRequest other = (ListVoicesRequest) o;
        return 
            Utils.enhancedDeepEquals(this.apiVersion, other.apiVersion) &&
            Utils.enhancedDeepEquals(this.accent, other.accent) &&
            Utils.enhancedDeepEquals(this.contexts, other.contexts) &&
            Utils.enhancedDeepEquals(this.gender, other.gender) &&
            Utils.enhancedDeepEquals(this.languageCode, other.languageCode) &&
            Utils.enhancedDeepEquals(this.pageSize, other.pageSize) &&
            Utils.enhancedDeepEquals(this.pageToken, other.pageToken) &&
            Utils.enhancedDeepEquals(this.persona, other.persona) &&
            Utils.enhancedDeepEquals(this.pitch, other.pitch) &&
            Utils.enhancedDeepEquals(this.regionCode, other.regionCode) &&
            Utils.enhancedDeepEquals(this.search, other.search) &&
            Utils.enhancedDeepEquals(this.type, other.type);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            apiVersion, accent, contexts,
            gender, languageCode, pageSize,
            pageToken, persona, pitch,
            regionCode, search, type);
    }
    
    @Override
    public String toString() {
        return Utils.toString(ListVoicesRequest.class,
                "apiVersion", apiVersion,
                "accent", accent,
                "contexts", contexts,
                "gender", gender,
                "languageCode", languageCode,
                "pageSize", pageSize,
                "pageToken", pageToken,
                "persona", persona,
                "pitch", pitch,
                "regionCode", regionCode,
                "search", search,
                "type", type);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private String apiVersion;

        private List<String> accent;

        private List<String> contexts;

        private List<String> gender;

        private List<String> languageCode;

        private Integer pageSize;

        private String pageToken;

        private List<String> persona;

        private List<String> pitch;

        private List<String> regionCode;

        private String search;

        private List<String> type;

        private Builder() {
          // force use of static builder() method
        }

        /**
         * API version for request routing.
         */
        public Builder apiVersion(@Nullable String apiVersion) {
            this.apiVersion = apiVersion;
            return this;
        }

        /**
         * Optional. Filter by accent description (e.g. "American", "British").
         * Case-insensitive exact match. If multiple values are specified, matches
         * voices with any of the specified accents (OR).
         */
        public Builder accent(@Nullable List<String> accent) {
            this.accent = accent;
            return this;
        }

        /**
         * Optional. Filter by intended context or domain (e.g. "News, Commercial").
         * Case-insensitive exact match. If multiple values are specified, matches
         * voices with any of the specified contexts (OR).
         */
        public Builder contexts(@Nullable List<String> contexts) {
            this.contexts = contexts;
            return this;
        }

        /**
         * Optional. Filter by gender presentation (e.g. "female", "male", "neutral").
         * Case-insensitive exact match. If multiple values are specified, matches
         * voices with any of the specified genders (OR).
         */
        public Builder gender(@Nullable List<String> gender) {
            this.gender = gender;
            return this;
        }

        /**
         * Optional. Filter by BCP-47 language code (e.g. "en-US").
         * Case-insensitive exact match. If multiple values are specified, matches
         * voices with any of the specified language codes (OR).
         */
        public Builder languageCode(@Nullable List<String> languageCode) {
            this.languageCode = languageCode;
            return this;
        }

        /**
         * Optional. The maximum number of voices to return per page. The service may
         * return fewer than this value. If unspecified, at most 50 voices are
         * returned. The maximum value is 1000; values above 1000 are coerced to
         * 1000.
         */
        public Builder pageSize(@Nullable Integer pageSize) {
            this.pageSize = pageSize;
            return this;
        }

        /**
         * Optional. A page token received from a previous `ListVoices` call. Provide
         * this to retrieve the subsequent page.
         * 
         * <p>When paginating, all filter query parameters (`language_code`,
         * `region_code`, `accent`, `persona`, `context`, `gender`, `pitch`, `type`,
         * and `search`) must match the call that returned this token; otherwise the
         * request fails with `INVALID_ARGUMENT`. `page_size` may change between
         * pages.
         */
        public Builder pageToken(@Nullable String pageToken) {
            this.pageToken = pageToken;
            return this;
        }

        /**
         * Optional. Filter by vocal persona (e.g. "Warm, Friendly").
         * Case-insensitive exact match. If multiple values are specified, matches
         * voices with any of the specified personas (OR).
         */
        public Builder persona(@Nullable List<String> persona) {
            this.persona = persona;
            return this;
        }

        /**
         * Optional. Filter by vocal pitch. Accepts `"low"`, `"medium"`, `"high"`
         * (case-insensitive). If multiple values are specified, matches voices with
         * any of the specified pitches (OR).
         */
        public Builder pitch(@Nullable List<String> pitch) {
            this.pitch = pitch;
            return this;
        }

        /**
         * Optional. Filter by ISO 3166-1 alpha-2 or UN M.49 region code (e.g. "US",
         * "001").
         * Case-insensitive exact match. If multiple values are specified, matches
         * voices with any of the specified region codes (OR).
         */
        public Builder regionCode(@Nullable List<String> regionCode) {
            this.regionCode = regionCode;
            return this;
        }

        /**
         * Optional. Free-text substring search query matched case-insensitively
         * against both `display_name` and `description`. Maximum 2048 bytes.
         */
        public Builder search(@Nullable String search) {
            this.search = search;
            return this;
        }

        /**
         * Optional. Filter by voice type. Accepts `"prebuilt"`, `"replicated"`,
         * `"prompted"` (case-insensitive). If multiple values are specified, matches
         * voices with any of the specified types (OR).
         */
        public Builder type(@Nullable List<String> type) {
            this.type = type;
            return this;
        }

        public ListVoicesRequest build() {
            return new ListVoicesRequest(
                apiVersion, accent, contexts,
                gender, languageCode, pageSize,
                pageToken, persona, pitch,
                regionCode, search, type);
        }

    }
}
