/*
* Copyright 2025 Google LLC
*
* Licensed under the Apache License, Version 2.0 (the "License");
* you may not use this file except in compliance with the License.
* You may obtain a copy of the License at
*
*     http://www.apache.org/licenses/LICENSE-2.0
*
* Unless required by applicable law or agreed to in writing, software
* distributed under the License is distributed on an "AS IS" BASIS,
* WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
* See the License for the specific language governing permissions and
* limitations under the License.
*/

// File generated from our OpenAPI spec by Stainless.

package com.google.genai.interactions.models.interactions

import com.fasterxml.jackson.core.JsonGenerator
import com.fasterxml.jackson.core.ObjectCodec
import com.fasterxml.jackson.databind.JsonNode
import com.fasterxml.jackson.databind.SerializerProvider
import com.fasterxml.jackson.databind.annotation.JsonDeserialize
import com.fasterxml.jackson.databind.annotation.JsonSerialize
import com.fasterxml.jackson.module.kotlin.jacksonTypeRef
import com.google.genai.interactions.core.BaseDeserializer
import com.google.genai.interactions.core.BaseSerializer
import com.google.genai.interactions.core.JsonValue
import com.google.genai.interactions.core.getOrThrow
import com.google.genai.interactions.errors.GeminiNextGenApiInvalidDataException
import java.util.Objects
import java.util.Optional
import kotlin.jvm.optionals.getOrNull

/** A step in the interaction. */
@JsonDeserialize(using = Step.Deserializer::class)
@JsonSerialize(using = Step.Serializer::class)
class Step
private constructor(
    private val userInput: UserInputStep? = null,
    private val modelOutput: ModelOutputStep? = null,
    private val thought: ThoughtStep? = null,
    private val functionCall: FunctionCallStep? = null,
    private val codeExecutionCall: CodeExecutionCallStep? = null,
    private val urlContextCall: UrlContextCallStep? = null,
    private val mcpServerToolCall: McpServerToolCallStep? = null,
    private val googleSearchCall: GoogleSearchCallStep? = null,
    private val fileSearchCall: FileSearchCallStep? = null,
    private val googleMapsCall: GoogleMapsCallStep? = null,
    private val functionResult: FunctionResultStep? = null,
    private val codeExecutionResult: CodeExecutionResultStep? = null,
    private val urlContextResult: UrlContextResultStep? = null,
    private val googleSearchResult: GoogleSearchResultStep? = null,
    private val mcpServerToolResult: McpServerToolResultStep? = null,
    private val fileSearchResult: FileSearchResultStep? = null,
    private val googleMapsResult: GoogleMapsResultStep? = null,
    private val _json: JsonValue? = null,
) {

    /** Input provided by the user. */
    fun userInput(): Optional<UserInputStep> = Optional.ofNullable(userInput)

    /** Output generated by the model. */
    fun modelOutput(): Optional<ModelOutputStep> = Optional.ofNullable(modelOutput)

    /** A thought step. */
    fun thought(): Optional<ThoughtStep> = Optional.ofNullable(thought)

    /** A function tool call step. */
    fun functionCall(): Optional<FunctionCallStep> = Optional.ofNullable(functionCall)

    /** Code execution call step. */
    fun codeExecutionCall(): Optional<CodeExecutionCallStep> =
        Optional.ofNullable(codeExecutionCall)

    /** URL context call step. */
    fun urlContextCall(): Optional<UrlContextCallStep> = Optional.ofNullable(urlContextCall)

    /** MCPServer tool call step. */
    fun mcpServerToolCall(): Optional<McpServerToolCallStep> =
        Optional.ofNullable(mcpServerToolCall)

    /** Google Search call step. */
    fun googleSearchCall(): Optional<GoogleSearchCallStep> = Optional.ofNullable(googleSearchCall)

    /** File Search call step. */
    fun fileSearchCall(): Optional<FileSearchCallStep> = Optional.ofNullable(fileSearchCall)

    /** Google Maps call step. */
    fun googleMapsCall(): Optional<GoogleMapsCallStep> = Optional.ofNullable(googleMapsCall)

    /** Result of a function tool call. */
    fun functionResult(): Optional<FunctionResultStep> = Optional.ofNullable(functionResult)

    /** Code execution result step. */
    fun codeExecutionResult(): Optional<CodeExecutionResultStep> =
        Optional.ofNullable(codeExecutionResult)

    /** URL context result step. */
    fun urlContextResult(): Optional<UrlContextResultStep> = Optional.ofNullable(urlContextResult)

    /** Google Search result step. */
    fun googleSearchResult(): Optional<GoogleSearchResultStep> =
        Optional.ofNullable(googleSearchResult)

    /** MCPServer tool result step. */
    fun mcpServerToolResult(): Optional<McpServerToolResultStep> =
        Optional.ofNullable(mcpServerToolResult)

    /** File Search result step. */
    fun fileSearchResult(): Optional<FileSearchResultStep> = Optional.ofNullable(fileSearchResult)

    /** Google Maps result step. */
    fun googleMapsResult(): Optional<GoogleMapsResultStep> = Optional.ofNullable(googleMapsResult)

    fun isUserInput(): Boolean = userInput != null

    fun isModelOutput(): Boolean = modelOutput != null

    fun isThought(): Boolean = thought != null

    fun isFunctionCall(): Boolean = functionCall != null

    fun isCodeExecutionCall(): Boolean = codeExecutionCall != null

    fun isUrlContextCall(): Boolean = urlContextCall != null

    fun isMcpServerToolCall(): Boolean = mcpServerToolCall != null

    fun isGoogleSearchCall(): Boolean = googleSearchCall != null

    fun isFileSearchCall(): Boolean = fileSearchCall != null

    fun isGoogleMapsCall(): Boolean = googleMapsCall != null

    fun isFunctionResult(): Boolean = functionResult != null

    fun isCodeExecutionResult(): Boolean = codeExecutionResult != null

    fun isUrlContextResult(): Boolean = urlContextResult != null

    fun isGoogleSearchResult(): Boolean = googleSearchResult != null

    fun isMcpServerToolResult(): Boolean = mcpServerToolResult != null

    fun isFileSearchResult(): Boolean = fileSearchResult != null

    fun isGoogleMapsResult(): Boolean = googleMapsResult != null

    /** Input provided by the user. */
    fun asUserInput(): UserInputStep = userInput.getOrThrow("userInput")

    /** Output generated by the model. */
    fun asModelOutput(): ModelOutputStep = modelOutput.getOrThrow("modelOutput")

    /** A thought step. */
    fun asThought(): ThoughtStep = thought.getOrThrow("thought")

    /** A function tool call step. */
    fun asFunctionCall(): FunctionCallStep = functionCall.getOrThrow("functionCall")

    /** Code execution call step. */
    fun asCodeExecutionCall(): CodeExecutionCallStep =
        codeExecutionCall.getOrThrow("codeExecutionCall")

    /** URL context call step. */
    fun asUrlContextCall(): UrlContextCallStep = urlContextCall.getOrThrow("urlContextCall")

    /** MCPServer tool call step. */
    fun asMcpServerToolCall(): McpServerToolCallStep =
        mcpServerToolCall.getOrThrow("mcpServerToolCall")

    /** Google Search call step. */
    fun asGoogleSearchCall(): GoogleSearchCallStep = googleSearchCall.getOrThrow("googleSearchCall")

    /** File Search call step. */
    fun asFileSearchCall(): FileSearchCallStep = fileSearchCall.getOrThrow("fileSearchCall")

    /** Google Maps call step. */
    fun asGoogleMapsCall(): GoogleMapsCallStep = googleMapsCall.getOrThrow("googleMapsCall")

    /** Result of a function tool call. */
    fun asFunctionResult(): FunctionResultStep = functionResult.getOrThrow("functionResult")

    /** Code execution result step. */
    fun asCodeExecutionResult(): CodeExecutionResultStep =
        codeExecutionResult.getOrThrow("codeExecutionResult")

    /** URL context result step. */
    fun asUrlContextResult(): UrlContextResultStep = urlContextResult.getOrThrow("urlContextResult")

    /** Google Search result step. */
    fun asGoogleSearchResult(): GoogleSearchResultStep =
        googleSearchResult.getOrThrow("googleSearchResult")

    /** MCPServer tool result step. */
    fun asMcpServerToolResult(): McpServerToolResultStep =
        mcpServerToolResult.getOrThrow("mcpServerToolResult")

    /** File Search result step. */
    fun asFileSearchResult(): FileSearchResultStep = fileSearchResult.getOrThrow("fileSearchResult")

    /** Google Maps result step. */
    fun asGoogleMapsResult(): GoogleMapsResultStep = googleMapsResult.getOrThrow("googleMapsResult")

    fun _json(): Optional<JsonValue> = Optional.ofNullable(_json)

    fun <T> accept(visitor: Visitor<T>): T =
        when {
            userInput != null -> visitor.visitUserInput(userInput)
            modelOutput != null -> visitor.visitModelOutput(modelOutput)
            thought != null -> visitor.visitThought(thought)
            functionCall != null -> visitor.visitFunctionCall(functionCall)
            codeExecutionCall != null -> visitor.visitCodeExecutionCall(codeExecutionCall)
            urlContextCall != null -> visitor.visitUrlContextCall(urlContextCall)
            mcpServerToolCall != null -> visitor.visitMcpServerToolCall(mcpServerToolCall)
            googleSearchCall != null -> visitor.visitGoogleSearchCall(googleSearchCall)
            fileSearchCall != null -> visitor.visitFileSearchCall(fileSearchCall)
            googleMapsCall != null -> visitor.visitGoogleMapsCall(googleMapsCall)
            functionResult != null -> visitor.visitFunctionResult(functionResult)
            codeExecutionResult != null -> visitor.visitCodeExecutionResult(codeExecutionResult)
            urlContextResult != null -> visitor.visitUrlContextResult(urlContextResult)
            googleSearchResult != null -> visitor.visitGoogleSearchResult(googleSearchResult)
            mcpServerToolResult != null -> visitor.visitMcpServerToolResult(mcpServerToolResult)
            fileSearchResult != null -> visitor.visitFileSearchResult(fileSearchResult)
            googleMapsResult != null -> visitor.visitGoogleMapsResult(googleMapsResult)
            else -> visitor.unknown(_json)
        }

    private var validated: Boolean = false

    fun validate(): Step = apply {
        if (validated) {
            return@apply
        }

        accept(
            object : Visitor<Unit> {
                override fun visitUserInput(userInput: UserInputStep) {
                    userInput.validate()
                }

                override fun visitModelOutput(modelOutput: ModelOutputStep) {
                    modelOutput.validate()
                }

                override fun visitThought(thought: ThoughtStep) {
                    thought.validate()
                }

                override fun visitFunctionCall(functionCall: FunctionCallStep) {
                    functionCall.validate()
                }

                override fun visitCodeExecutionCall(codeExecutionCall: CodeExecutionCallStep) {
                    codeExecutionCall.validate()
                }

                override fun visitUrlContextCall(urlContextCall: UrlContextCallStep) {
                    urlContextCall.validate()
                }

                override fun visitMcpServerToolCall(mcpServerToolCall: McpServerToolCallStep) {
                    mcpServerToolCall.validate()
                }

                override fun visitGoogleSearchCall(googleSearchCall: GoogleSearchCallStep) {
                    googleSearchCall.validate()
                }

                override fun visitFileSearchCall(fileSearchCall: FileSearchCallStep) {
                    fileSearchCall.validate()
                }

                override fun visitGoogleMapsCall(googleMapsCall: GoogleMapsCallStep) {
                    googleMapsCall.validate()
                }

                override fun visitFunctionResult(functionResult: FunctionResultStep) {
                    functionResult.validate()
                }

                override fun visitCodeExecutionResult(
                    codeExecutionResult: CodeExecutionResultStep
                ) {
                    codeExecutionResult.validate()
                }

                override fun visitUrlContextResult(urlContextResult: UrlContextResultStep) {
                    urlContextResult.validate()
                }

                override fun visitGoogleSearchResult(googleSearchResult: GoogleSearchResultStep) {
                    googleSearchResult.validate()
                }

                override fun visitMcpServerToolResult(
                    mcpServerToolResult: McpServerToolResultStep
                ) {
                    mcpServerToolResult.validate()
                }

                override fun visitFileSearchResult(fileSearchResult: FileSearchResultStep) {
                    fileSearchResult.validate()
                }

                override fun visitGoogleMapsResult(googleMapsResult: GoogleMapsResultStep) {
                    googleMapsResult.validate()
                }
            }
        )
        validated = true
    }

    fun isValid(): Boolean =
        try {
            validate()
            true
        } catch (e: GeminiNextGenApiInvalidDataException) {
            false
        }

    /**
     * Returns a score indicating how many valid values are contained in this object recursively.
     *
     * Used for best match union deserialization.
     */
    @JvmSynthetic
    internal fun validity(): Int =
        accept(
            object : Visitor<Int> {
                override fun visitUserInput(userInput: UserInputStep) = userInput.validity()

                override fun visitModelOutput(modelOutput: ModelOutputStep) = modelOutput.validity()

                override fun visitThought(thought: ThoughtStep) = thought.validity()

                override fun visitFunctionCall(functionCall: FunctionCallStep) =
                    functionCall.validity()

                override fun visitCodeExecutionCall(codeExecutionCall: CodeExecutionCallStep) =
                    codeExecutionCall.validity()

                override fun visitUrlContextCall(urlContextCall: UrlContextCallStep) =
                    urlContextCall.validity()

                override fun visitMcpServerToolCall(mcpServerToolCall: McpServerToolCallStep) =
                    mcpServerToolCall.validity()

                override fun visitGoogleSearchCall(googleSearchCall: GoogleSearchCallStep) =
                    googleSearchCall.validity()

                override fun visitFileSearchCall(fileSearchCall: FileSearchCallStep) =
                    fileSearchCall.validity()

                override fun visitGoogleMapsCall(googleMapsCall: GoogleMapsCallStep) =
                    googleMapsCall.validity()

                override fun visitFunctionResult(functionResult: FunctionResultStep) =
                    functionResult.validity()

                override fun visitCodeExecutionResult(
                    codeExecutionResult: CodeExecutionResultStep
                ) = codeExecutionResult.validity()

                override fun visitUrlContextResult(urlContextResult: UrlContextResultStep) =
                    urlContextResult.validity()

                override fun visitGoogleSearchResult(googleSearchResult: GoogleSearchResultStep) =
                    googleSearchResult.validity()

                override fun visitMcpServerToolResult(
                    mcpServerToolResult: McpServerToolResultStep
                ) = mcpServerToolResult.validity()

                override fun visitFileSearchResult(fileSearchResult: FileSearchResultStep) =
                    fileSearchResult.validity()

                override fun visitGoogleMapsResult(googleMapsResult: GoogleMapsResultStep) =
                    googleMapsResult.validity()

                override fun unknown(json: JsonValue?) = 0
            }
        )

    override fun equals(other: Any?): Boolean {
        if (this === other) {
            return true
        }

        return other is Step &&
            userInput == other.userInput &&
            modelOutput == other.modelOutput &&
            thought == other.thought &&
            functionCall == other.functionCall &&
            codeExecutionCall == other.codeExecutionCall &&
            urlContextCall == other.urlContextCall &&
            mcpServerToolCall == other.mcpServerToolCall &&
            googleSearchCall == other.googleSearchCall &&
            fileSearchCall == other.fileSearchCall &&
            googleMapsCall == other.googleMapsCall &&
            functionResult == other.functionResult &&
            codeExecutionResult == other.codeExecutionResult &&
            urlContextResult == other.urlContextResult &&
            googleSearchResult == other.googleSearchResult &&
            mcpServerToolResult == other.mcpServerToolResult &&
            fileSearchResult == other.fileSearchResult &&
            googleMapsResult == other.googleMapsResult
    }

    override fun hashCode(): Int =
        Objects.hash(
            userInput,
            modelOutput,
            thought,
            functionCall,
            codeExecutionCall,
            urlContextCall,
            mcpServerToolCall,
            googleSearchCall,
            fileSearchCall,
            googleMapsCall,
            functionResult,
            codeExecutionResult,
            urlContextResult,
            googleSearchResult,
            mcpServerToolResult,
            fileSearchResult,
            googleMapsResult,
        )

    override fun toString(): String =
        when {
            userInput != null -> "Step{userInput=$userInput}"
            modelOutput != null -> "Step{modelOutput=$modelOutput}"
            thought != null -> "Step{thought=$thought}"
            functionCall != null -> "Step{functionCall=$functionCall}"
            codeExecutionCall != null -> "Step{codeExecutionCall=$codeExecutionCall}"
            urlContextCall != null -> "Step{urlContextCall=$urlContextCall}"
            mcpServerToolCall != null -> "Step{mcpServerToolCall=$mcpServerToolCall}"
            googleSearchCall != null -> "Step{googleSearchCall=$googleSearchCall}"
            fileSearchCall != null -> "Step{fileSearchCall=$fileSearchCall}"
            googleMapsCall != null -> "Step{googleMapsCall=$googleMapsCall}"
            functionResult != null -> "Step{functionResult=$functionResult}"
            codeExecutionResult != null -> "Step{codeExecutionResult=$codeExecutionResult}"
            urlContextResult != null -> "Step{urlContextResult=$urlContextResult}"
            googleSearchResult != null -> "Step{googleSearchResult=$googleSearchResult}"
            mcpServerToolResult != null -> "Step{mcpServerToolResult=$mcpServerToolResult}"
            fileSearchResult != null -> "Step{fileSearchResult=$fileSearchResult}"
            googleMapsResult != null -> "Step{googleMapsResult=$googleMapsResult}"
            _json != null -> "Step{_unknown=$_json}"
            else -> throw IllegalStateException("Invalid Step")
        }

    companion object {

        /** Input provided by the user. */
        @JvmStatic fun ofUserInput(userInput: UserInputStep) = Step(userInput = userInput)

        /** Output generated by the model. */
        @JvmStatic fun ofModelOutput(modelOutput: ModelOutputStep) = Step(modelOutput = modelOutput)

        /** A thought step. */
        @JvmStatic fun ofThought(thought: ThoughtStep) = Step(thought = thought)

        /** A function tool call step. */
        @JvmStatic
        fun ofFunctionCall(functionCall: FunctionCallStep) = Step(functionCall = functionCall)

        /** Code execution call step. */
        @JvmStatic
        fun ofCodeExecutionCall(codeExecutionCall: CodeExecutionCallStep) =
            Step(codeExecutionCall = codeExecutionCall)

        /** URL context call step. */
        @JvmStatic
        fun ofUrlContextCall(urlContextCall: UrlContextCallStep) =
            Step(urlContextCall = urlContextCall)

        /** MCPServer tool call step. */
        @JvmStatic
        fun ofMcpServerToolCall(mcpServerToolCall: McpServerToolCallStep) =
            Step(mcpServerToolCall = mcpServerToolCall)

        /** Google Search call step. */
        @JvmStatic
        fun ofGoogleSearchCall(googleSearchCall: GoogleSearchCallStep) =
            Step(googleSearchCall = googleSearchCall)

        /** File Search call step. */
        @JvmStatic
        fun ofFileSearchCall(fileSearchCall: FileSearchCallStep) =
            Step(fileSearchCall = fileSearchCall)

        /** Google Maps call step. */
        @JvmStatic
        fun ofGoogleMapsCall(googleMapsCall: GoogleMapsCallStep) =
            Step(googleMapsCall = googleMapsCall)

        /** Result of a function tool call. */
        @JvmStatic
        fun ofFunctionResult(functionResult: FunctionResultStep) =
            Step(functionResult = functionResult)

        /** Code execution result step. */
        @JvmStatic
        fun ofCodeExecutionResult(codeExecutionResult: CodeExecutionResultStep) =
            Step(codeExecutionResult = codeExecutionResult)

        /** URL context result step. */
        @JvmStatic
        fun ofUrlContextResult(urlContextResult: UrlContextResultStep) =
            Step(urlContextResult = urlContextResult)

        /** Google Search result step. */
        @JvmStatic
        fun ofGoogleSearchResult(googleSearchResult: GoogleSearchResultStep) =
            Step(googleSearchResult = googleSearchResult)

        /** MCPServer tool result step. */
        @JvmStatic
        fun ofMcpServerToolResult(mcpServerToolResult: McpServerToolResultStep) =
            Step(mcpServerToolResult = mcpServerToolResult)

        /** File Search result step. */
        @JvmStatic
        fun ofFileSearchResult(fileSearchResult: FileSearchResultStep) =
            Step(fileSearchResult = fileSearchResult)

        /** Google Maps result step. */
        @JvmStatic
        fun ofGoogleMapsResult(googleMapsResult: GoogleMapsResultStep) =
            Step(googleMapsResult = googleMapsResult)
    }

    /** An interface that defines how to map each variant of [Step] to a value of type [T]. */
    interface Visitor<out T> {

        /** Input provided by the user. */
        fun visitUserInput(userInput: UserInputStep): T

        /** Output generated by the model. */
        fun visitModelOutput(modelOutput: ModelOutputStep): T

        /** A thought step. */
        fun visitThought(thought: ThoughtStep): T

        /** A function tool call step. */
        fun visitFunctionCall(functionCall: FunctionCallStep): T

        /** Code execution call step. */
        fun visitCodeExecutionCall(codeExecutionCall: CodeExecutionCallStep): T

        /** URL context call step. */
        fun visitUrlContextCall(urlContextCall: UrlContextCallStep): T

        /** MCPServer tool call step. */
        fun visitMcpServerToolCall(mcpServerToolCall: McpServerToolCallStep): T

        /** Google Search call step. */
        fun visitGoogleSearchCall(googleSearchCall: GoogleSearchCallStep): T

        /** File Search call step. */
        fun visitFileSearchCall(fileSearchCall: FileSearchCallStep): T

        /** Google Maps call step. */
        fun visitGoogleMapsCall(googleMapsCall: GoogleMapsCallStep): T

        /** Result of a function tool call. */
        fun visitFunctionResult(functionResult: FunctionResultStep): T

        /** Code execution result step. */
        fun visitCodeExecutionResult(codeExecutionResult: CodeExecutionResultStep): T

        /** URL context result step. */
        fun visitUrlContextResult(urlContextResult: UrlContextResultStep): T

        /** Google Search result step. */
        fun visitGoogleSearchResult(googleSearchResult: GoogleSearchResultStep): T

        /** MCPServer tool result step. */
        fun visitMcpServerToolResult(mcpServerToolResult: McpServerToolResultStep): T

        /** File Search result step. */
        fun visitFileSearchResult(fileSearchResult: FileSearchResultStep): T

        /** Google Maps result step. */
        fun visitGoogleMapsResult(googleMapsResult: GoogleMapsResultStep): T

        /**
         * Maps an unknown variant of [Step] to a value of type [T].
         *
         * An instance of [Step] can contain an unknown variant if it was deserialized from data
         * that doesn't match any known variant. For example, if the SDK is on an older version than
         * the API, then the API may respond with new variants that the SDK is unaware of.
         *
         * @throws GeminiNextGenApiInvalidDataException in the default implementation.
         */
        fun unknown(json: JsonValue?): T {
            throw GeminiNextGenApiInvalidDataException("Unknown Step: $json")
        }
    }

    internal class Deserializer : BaseDeserializer<Step>(Step::class) {

        override fun ObjectCodec.deserialize(node: JsonNode): Step {
            val json = JsonValue.fromJsonNode(node)
            val type = json.asObject().getOrNull()?.get("type")?.asString()?.getOrNull()

            when (type) {
                "user_input" -> {
                    return tryDeserialize(node, jacksonTypeRef<UserInputStep>())?.let {
                        Step(userInput = it, _json = json)
                    } ?: Step(_json = json)
                }
                "model_output" -> {
                    return tryDeserialize(node, jacksonTypeRef<ModelOutputStep>())?.let {
                        Step(modelOutput = it, _json = json)
                    } ?: Step(_json = json)
                }
                "thought" -> {
                    return tryDeserialize(node, jacksonTypeRef<ThoughtStep>())?.let {
                        Step(thought = it, _json = json)
                    } ?: Step(_json = json)
                }
                "function_call" -> {
                    return tryDeserialize(node, jacksonTypeRef<FunctionCallStep>())?.let {
                        Step(functionCall = it, _json = json)
                    } ?: Step(_json = json)
                }
                "code_execution_call" -> {
                    return tryDeserialize(node, jacksonTypeRef<CodeExecutionCallStep>())?.let {
                        Step(codeExecutionCall = it, _json = json)
                    } ?: Step(_json = json)
                }
                "url_context_call" -> {
                    return tryDeserialize(node, jacksonTypeRef<UrlContextCallStep>())?.let {
                        Step(urlContextCall = it, _json = json)
                    } ?: Step(_json = json)
                }
                "mcp_server_tool_call" -> {
                    return tryDeserialize(node, jacksonTypeRef<McpServerToolCallStep>())?.let {
                        Step(mcpServerToolCall = it, _json = json)
                    } ?: Step(_json = json)
                }
                "google_search_call" -> {
                    return tryDeserialize(node, jacksonTypeRef<GoogleSearchCallStep>())?.let {
                        Step(googleSearchCall = it, _json = json)
                    } ?: Step(_json = json)
                }
                "file_search_call" -> {
                    return tryDeserialize(node, jacksonTypeRef<FileSearchCallStep>())?.let {
                        Step(fileSearchCall = it, _json = json)
                    } ?: Step(_json = json)
                }
                "google_maps_call" -> {
                    return tryDeserialize(node, jacksonTypeRef<GoogleMapsCallStep>())?.let {
                        Step(googleMapsCall = it, _json = json)
                    } ?: Step(_json = json)
                }
                "function_result" -> {
                    return tryDeserialize(node, jacksonTypeRef<FunctionResultStep>())?.let {
                        Step(functionResult = it, _json = json)
                    } ?: Step(_json = json)
                }
                "code_execution_result" -> {
                    return tryDeserialize(node, jacksonTypeRef<CodeExecutionResultStep>())?.let {
                        Step(codeExecutionResult = it, _json = json)
                    } ?: Step(_json = json)
                }
                "url_context_result" -> {
                    return tryDeserialize(node, jacksonTypeRef<UrlContextResultStep>())?.let {
                        Step(urlContextResult = it, _json = json)
                    } ?: Step(_json = json)
                }
                "google_search_result" -> {
                    return tryDeserialize(node, jacksonTypeRef<GoogleSearchResultStep>())?.let {
                        Step(googleSearchResult = it, _json = json)
                    } ?: Step(_json = json)
                }
                "mcp_server_tool_result" -> {
                    return tryDeserialize(node, jacksonTypeRef<McpServerToolResultStep>())?.let {
                        Step(mcpServerToolResult = it, _json = json)
                    } ?: Step(_json = json)
                }
                "file_search_result" -> {
                    return tryDeserialize(node, jacksonTypeRef<FileSearchResultStep>())?.let {
                        Step(fileSearchResult = it, _json = json)
                    } ?: Step(_json = json)
                }
                "google_maps_result" -> {
                    return tryDeserialize(node, jacksonTypeRef<GoogleMapsResultStep>())?.let {
                        Step(googleMapsResult = it, _json = json)
                    } ?: Step(_json = json)
                }
            }

            return Step(_json = json)
        }
    }

    internal class Serializer : BaseSerializer<Step>(Step::class) {

        override fun serialize(
            value: Step,
            generator: JsonGenerator,
            provider: SerializerProvider,
        ) {
            when {
                value.userInput != null -> generator.writeObject(value.userInput)
                value.modelOutput != null -> generator.writeObject(value.modelOutput)
                value.thought != null -> generator.writeObject(value.thought)
                value.functionCall != null -> generator.writeObject(value.functionCall)
                value.codeExecutionCall != null -> generator.writeObject(value.codeExecutionCall)
                value.urlContextCall != null -> generator.writeObject(value.urlContextCall)
                value.mcpServerToolCall != null -> generator.writeObject(value.mcpServerToolCall)
                value.googleSearchCall != null -> generator.writeObject(value.googleSearchCall)
                value.fileSearchCall != null -> generator.writeObject(value.fileSearchCall)
                value.googleMapsCall != null -> generator.writeObject(value.googleMapsCall)
                value.functionResult != null -> generator.writeObject(value.functionResult)
                value.codeExecutionResult != null ->
                    generator.writeObject(value.codeExecutionResult)
                value.urlContextResult != null -> generator.writeObject(value.urlContextResult)
                value.googleSearchResult != null -> generator.writeObject(value.googleSearchResult)
                value.mcpServerToolResult != null ->
                    generator.writeObject(value.mcpServerToolResult)
                value.fileSearchResult != null -> generator.writeObject(value.fileSearchResult)
                value.googleMapsResult != null -> generator.writeObject(value.googleMapsResult)
                value._json != null -> generator.writeObject(value._json)
                else -> throw IllegalStateException("Invalid Step")
            }
        }
    }
}
